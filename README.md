# dbt_scooters 🛴

Проект трансформації даних для аналітики сервісу оренди самокатів. Побудований на dbt (data build tool) з використанням бази даних PostgreSQL.

Для керування залежностями використовується [uv]

## 🚀 Швидкий старт

### 1. Встановлення оточення
Переконайтеся, що у вас встановлений `uv`.

```bash
uv venv

.venv\Scripts\activate

uv pip install dbt-core dbt-postgres

Ось лаконічний шаблон README.md для вашого проекту. Ви можете скопіювати цей текст і вставити його у файл README.md у корені вашої папки dbt_scooters.

Markdown

# dbt_scooters 🛴

Проект трансформації даних для аналітики сервісу оренди самокатів. Побудований на **dbt (data build tool)** з використанням бази даних **PostgreSQL**.

Для керування залежностями використовується [uv](https://github.com/astral-sh/uv).

## 🚀 Швидкий старт

### 1. Встановлення оточення
Переконайтеся, що у вас встановлений `uv`.

# Створити віртуальне середовище
uv venv

# Активувати середовище (Windows)
.venv\Scripts\activate

# Встановити dbt та адаптер для Postgres
uv pip install dbt-core dbt-postgres
2. Налаштування підключення
Створіть або відредагуйте файл profiles.yml (зазвичай у C:\Users\<User>\.dbt\). Переконайтеся, що профіль dbt_scooters налаштований коректно (host, user, password від Render).

Перевірка з'єднання:

dbt debug

Команда,            Опис
dbt debug,          Перевірка з'єднання з базою даних та файлу profiles.yml.
dbt run,            Запуск усіх моделей (створення таблиць/views у базі).
dbt test,           "Запуск тестів даних (unique, not_null тощо)."
dbt build,          Рекомендовано. Виконує run + test + seed разом.
dbt seed,           Завантаження CSV-файлів з папки /seeds у базу даних.
dbt docs generate,  Генерація документації проекту.
dbt docs serve,     Запуск веб-сайту з документацією та лініджем (lineage).
dbt clean,          Очищення папок target/ та dbt_packages/ (якщо виникають дивні помилки).